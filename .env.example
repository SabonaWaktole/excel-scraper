EMAIL=your@email.com
PASSWORD=your_password
BASE_URL=https://excelcommunityliving.website

# Set to 1 to keep existing output/ between runs (default: full wipe each run).
# SKIP_OUTPUT_RESET=1

# Optional: full Tutor LMS course archive URL (page 1). After each page, the scraper clicks the pagination "Next" chevron and waits for tutor_course_filter_ajax.
# If unset, defaults to: /courses/?course_per_page=12&show_pagination=1&current_page=1
# COURSES_LIST_URL=https://excelcommunityliving.website/courses/?course_per_page=12&show_pagination=1&current_page=1
